{
  "name": "taetris-stun-client",
  "version": "1.0.0",
  "main": "dist/index.html",
  "repository": "https://github.com/taetris-stun/taetris-stun.github.io",
  "author": "tinkertoe",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel serve src/index.html",
    "build": "rm -rf dist && parcel build --no-source-maps src/index.html",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/transformer-vue": "2.0.0-beta.2",
    "gh-pages": "^3.2.0",
    "parcel": "^2.0.0-beta.2"
  },
  "dependencies": {
    "jquery": "^3.6.0",
    "vue": "^3.0.11",
    "vue-router": "4"
  }
}
